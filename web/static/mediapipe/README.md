# MediaPipe assets (guest background blur)

Vendored for local serving so guest browsers do not depend on a third-party CDN at runtime.

| Asset | Source | Licence |
|-------|--------|---------|
| `*.wasm`, `*.js` (vision tasks runtime) | `@mediapipe/tasks-vision` npm | Apache-2.0 |
| `selfie_segmenter.tflite` | MediaPipe Image Segmenter (selfie) float16 | Apache-2.0 |

See https://developers.google.com/mediapipe and the package privacy notice.
